[package]
name = "kiri-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
cobs = { version = "0.1.4", default-features = false }
packed_struct = { version = "0.10", default-features = false }

heapless = "0.7"

crc = "2.1"